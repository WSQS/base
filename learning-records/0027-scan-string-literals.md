# Scan string literals

用户已经把语言的字符串支持从词法层最小地向前推进了一步：scanner 现在能够把双引号包裹的文本识别为单个 `Token::String(...)`，并通过测试验证了它不会被误扫成标识符、关键字或数字。这意味着后续把字符串接进 parser / AST / evaluator 时，可以建立在已经稳定的字符串词法入口之上继续推进，而不必把字符串的多个实现阶段混在同一课里完成。
