# Evaluate comparison expressions

用户已经把比较表达式从 parser 层继续推进到了 evaluator 层：当左右两边是整数时，`<`、`>`、`<=`、`>=`、`==`、`!=` 现在会返回 `Value::Boolean`，并通过程序级测试验证了布尔结果可以进入环境。这意味着语言现在第一次真正拥有了非整数的运行时结果，后续布尔字面量、条件语句与控制流都可以建立在这个值语义基础上继续扩展。