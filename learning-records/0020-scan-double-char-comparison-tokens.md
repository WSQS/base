# Scan double-character comparison tokens

用户已经把 scanner 从“只看当前字符”的逐字符分支，推进到了“必要时前瞻一个字符”的位置控制式扫描：`scan()` 现在能识别 `==`、`!=`、`<=`、`>=`，并用独立测试验证了双字符比较 token 的输出。这意味着后续继续扩展比较表达式时，词法层已经具备处理单字符与双字符运算符并存的基础能力。